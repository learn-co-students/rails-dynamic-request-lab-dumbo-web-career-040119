HI THIS IS AN INDEX PAGE

<div>

  <% @students.each do |student| %>
    <div>
      <ul>
        <li>
        <a href="/students/<%=student.id%>"> <%= student.first_name + " " + student.last_name%> </a>
        </li>
      </ul>
    </div>
  <% end %>

</div>
